# 의존성 라이브러리 (가장 중요: 용량이 크고 npm install로 재생성 가능)
node_modules/
dist/
dist-ssr/
*.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 환경 변수 (보안: API 키 등이 들어있으므로 절대 올리면 안 됨)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 에디터 설정 파일
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

